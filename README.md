# Firestore playground

A playground repo so I can start to understand how Firestore works. You will need to download a service account key and store in `/tmp/service-account.json`.

```shell
go build
./playgound-firestore
```
